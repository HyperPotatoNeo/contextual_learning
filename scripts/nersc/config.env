# NERSC Perlmutter Configuration for prime-rl with SkyRL container
# Source this file from other scripts: source "$(dirname "$0")/config.env"

# ============================================================================
# Container
# ============================================================================
SKYRL_IMAGE="docker.io/novaskyai/skyrl-train-ray-2.51.1-py3.12-cu12.8"
PODMANHPC_PODMAN_BIN="/global/common/shared/das/podman-4.7.0/bin/podman"

# ============================================================================
# Paths
# ============================================================================
REPO_DIR="${SCRATCH}/contextual_learning"
REPO_HOME_DIR="${HOME}/contextual_learning"

# Cache directories (on $SCRATCH for persistence and performance)
UV_CACHE="${SCRATCH}/uv_cache"
UV_PYTHON_DIR="${SCRATCH}/uv_python"
HF_CACHE="${SCRATCH}/hf_cache"
LOG_DIR="${REPO_DIR}/logs"

# ============================================================================
# SLURM defaults
# ============================================================================
SLURM_ACCOUNT="m5017"
SLURM_CONSTRAINT="gpu&hbm80g"
